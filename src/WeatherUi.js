import "./WeatherUi";
function WeatherUi({ data }) {
  return (
    <div className="container inner">
      {/* <h1>{data.name}</h1> */}
      {/* {data && data.main.temp && <h2>{data.main.temp}</h2>} */}
    </div>
  );
}
export default WeatherUi;
